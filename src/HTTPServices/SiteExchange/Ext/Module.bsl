
Функция XMLSchemaIndex(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);
	Ответ.Заголовки.Вставить("Content-Type", "application/xml");
	лЗапись = Новый ЗаписьXML;
	лЗапись.УстановитьСтроку("utf-8");
	ФабрикаXDTO.ЗаписатьXML(лЗапись, ФабрикаXDTO.ЭкспортМоделиXDTO("http://ws-02.part-kom.ru/partkom83/hs/SiteExchange/XMLSchema"));
	Ответ.УстановитьТелоИзСтроки(лЗапись.Закрыть(), "utf-8", ИспользованиеByteOrderMark.НеИспользовать);
	Возврат Ответ;
КонецФункции
