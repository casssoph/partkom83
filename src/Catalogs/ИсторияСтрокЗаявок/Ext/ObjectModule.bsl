Процедура ПриЗаписи(Отказ)
	
	Наименование = СтрокаЗаявки.IDSite;
	
	Если	РегистрыСведений.НастройкиПодсистем.ЗначениеПараметра("Обмен 1С-Сайт", "Справочник: ИсторияСтрокЗаявок", Ложь) И
			НЕ  ОбменДанными.Загрузка И	ЗначениеЗаполнено(СтрокаЗаявки) И
			НЕ СтрокаЗаявки.Виртуальная И ЗначениеЗаполнено(СтрокаЗаявки.Заявка) Тогда
			
			ОбменДаннымиКлиентСервер.ЗарегистрироватьОбъект(ЭтотОбъект, Метаданные.ПланыОбмена.ОбменПартКом83_Сайт_состояние_заявок);
			
	КонецЕсли;
	
КонецПроцедуры

