Процедура ВыполнитьРегламентноеЗадание() Экспорт
	
	Если НЕ ЗначениеЗаполнено(ИдентификаторУзлаОбмена) тогда
		ВызватьИсключение "Не заполнен реквизит <Узел обмена>";
	Иначе
		ПланыОбмена.ОбменПартКом83_ОкноПоставщикаПрайсыVMI.ЗагрузитьИзмененияОтУзла(ИдентификаторУзлаОбмена);
	КонецЕсли;
	
КонецПроцедуры

