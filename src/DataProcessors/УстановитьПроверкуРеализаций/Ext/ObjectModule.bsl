
Процедура ПередОткрытием(Отказ, СтандартнаяОбработка)
	
	Если НЕ ПодключитьВнешнююКомпоненту("AddIn.vk_rs232") тогда
		Предупреждение("Внешняя компонента не загружена.");
		Отказ = Истина;
	КонецЕсли;
	
КонецПроцедуры
