
&НаСервере
Процедура ОчиститьКэшНаСервере()
	
	НаборЗаписей = РегистрыСведений.внКэшЖурналаРегистрации.СоздатьНаборЗаписей();
	НаборЗаписей.Записать();
	
КонецПроцедуры

&НаКлиенте
Процедура ОчиститьКэш(Команда)
	
	ОчиститьКэшНаСервере();
	Оповестить("внОбновитьИсторию", , ЭтаФорма);
	
КонецПроцедуры
