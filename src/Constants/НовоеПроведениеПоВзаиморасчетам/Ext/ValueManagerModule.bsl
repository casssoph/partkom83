
Процедура ПриЗаписи(Отказ)
	глЗначениеПеременнойУстановить("НовоеПроведениеПоВзаиморасчетам", Значение, Истина);
КонецПроцедуры

Процедура ПередЗаписью(Отказ)
	СтароеЗначение = Константы.НовоеПроведениеПоВзаиморасчетам.Получить();
	Если Значение <> СтароеЗначение Тогда 
		Значение = Истина;
		Сообщить("Константу " + ЭтотОбъект.Метаданные().Синоним + " менять нельзя");
	КонецЕсли;
КонецПроцедуры
