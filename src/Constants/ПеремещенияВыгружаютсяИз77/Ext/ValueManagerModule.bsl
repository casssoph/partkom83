Процедура ПриЗаписи(Отказ)
	глЗначениеПеременнойУстановить("ПеремещенияВыгружаютсяИз77", Значение, Истина);
КонецПроцедуры

Процедура ПередЗаписью(Отказ)
	СтароеЗначение = Константы.ПеремещенияВыгружаютсяИз77.Получить();
	Если Значение <> СтароеЗначение Тогда 
		Значение = Истина;
		Сообщить("Константу " + ЭтотОбъект.Метаданные().Синоним + " менять нельзя");
	КонецЕсли;
КонецПроцедуры
