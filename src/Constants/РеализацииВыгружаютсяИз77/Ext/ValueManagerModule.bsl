Процедура ПриЗаписи(Отказ)
	глЗначениеПеременнойУстановить("РеализацииВыгружаютсяИз77", Значение, Истина);
КонецПроцедуры

Процедура ПередЗаписью(Отказ)
	СтароеЗначение = Константы.РеализацииВыгружаютсяИз77.Получить();
	Если Значение <> СтароеЗначение Тогда 
		Значение = Истина;
		Сообщить("Константу " + ЭтотОбъект.Метаданные().Синоним + " менять нельзя");
	КонецЕсли;
КонецПроцедуры

